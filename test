#!/bin/sh
[ -d output ] || exit 1
pout=output/test-py-output
rout=output/test-R-output
echo "fustal" > "$pout"
echo "R" > "$rout"
PYTHONPATH=$PWD/output ./test.py >> "$pout"
R --no-save < test.R | grep '^\[1\]' | cut -d']' -f2 | tr -d ' ' >> "$rout"
paste -d'\n' "$pout" "$rout" | paste - -
